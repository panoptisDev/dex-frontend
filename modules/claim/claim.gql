query GetUserBribeClaims($user: String!, $epoch: Int!) {
  getUserBribeClaims(user: $user, epoch: $epoch) {
    distributionId
    amountOwed
    briber
    gauge
    token
    proof
    valueUSD
    gaugeRecord {
      symbol
    }
    pool {
      name
    }
  }
}
